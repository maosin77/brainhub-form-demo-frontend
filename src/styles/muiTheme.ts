import { createTheme } from '@mui/material';

export const theme = createTheme();

theme.typography.h3 = {
    fontWeight: 600,
    fontSize: '1.3rem',
};

theme.typography.h4 = {
    fontWeight: 300,
    fontSize: '0.9rem',
};
